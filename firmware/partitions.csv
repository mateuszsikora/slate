# Slate partition table — ESP32-S3-WROOM-1-N16R8, 16 MB flash.
#
# Settled in issue #5 against the S-3 measurements (docs/spikes/s3.md); the
# reasoning behind every size is in design.md §6.3. Changing this table later
# forces a full serial flash, which is why it is written before firmware code.
#
# The table fills the flash exactly, from 0x9000 to 0x1000000. nvs absorbs the
# gap that 64 KB app-partition alignment would otherwise strand in front of
# ota_0 — the 80 KB is a consequence of that alignment, not a requirement.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x14000,
otadata,    data, ota,      0x1D000,  0x2000,
phy_init,   data, phy,      0x1F000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x600000,
ota_1,      app,  ota_1,    0x620000, 0x600000,
littlefs,   data, littlefs, 0xC20000, 0x3C0000,
coredump,   data, coredump, 0xFE0000, 0x20000,
