# ESP-IDF build output. Every run is its own build directory and its own
# sdkconfig — see main/CMakeLists.txt for why the explicit -DSDKCONFIG matters.
build*/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
*.buildlog
*.flashlog
__pycache__/
